{
  "paper": {
    "title": "Pythia: Exploiting Workflow Predictability for Efficient Agent-Native LLM Serving",
    "arxiv": "2604.25899",
    "venue": "UNVERIFIED"
  },
  "arm": "pythia_completion",
  "freeze": {
    "name": "Pythia Baseline Freeze v1",
    "head": "54a2d25088832049afb0cf860cdabfe5152c8c3d",
    "head_full_sha": "54a2d25088832049afb0cf860cdabfe5152c8c3d",
    "status": "APPROVED",
    "approval_quote": "可以正式 freeze",
    "gates": "tests/test_pythia_fidelity.py",
    "gate_count": 24,
    "declaration_received": true,
    "freeze_breaking_fix_commit": "54a2d25088832049afb0cf860cdabfe5152c8c3d"
  },
  "faithful": [
    "workflow profiler / probabilistic finite automaton over a ROLE alphabet",
    "a loop is expressed as a REPEATED role, so the occurrence suffix is dropped",
    "low-probability transition pruning as the bounded probable future",
    "expected remaining DISTANCE (number of future role transitions) from the automaton",
    "completion-aware priority S_completion = 1 / E[D_remaining] with omega1 = 1"
  ],
  "adapted": [
    "role key composed from role:action_family:raw_action, because the paper's alphabet entry is the current agent_id and a bare VideoSeek role is too coarse",
    "the finite horizon is OUR fixed, train-independent step limit and is NOT the paper's bound, which prunes transitions and applies repetition bounds to looping roles",
    "indexing by the candidate's own role (the agent_id Pythia exposes on a request)",
    "S_unblock approximated by a queue-demand PROXY (scheduler-visible ready+running GPU requests per model), because this simulator has no model-replica queue",
    "aging expressed as a dimensionless re-score S + lambda*(wait/tau); the paper's worker loop is not reproduced",
    "S_unblock is the mean idle-risk over PFA-reachable future roles, bounded in [0,1] so it is commensurate with S_completion"
  ],
  "deviation": [
    "H=6 fixed finite-horizon recursion replaces Pythia's bounded-regex / repetition-bound representation for expected remaining-distance estimation. The paper bounds its automaton with low-probability transition filtering plus repetition bounds on looping roles (engineer^{3,6}); H=6 is our fixed, train-independent approximation.",
    "S_unblock / DownstreamIdleRisk not migrated",
    "speculative and prefix cache routing, model-replica idleness, autoscaling, and the original serving/batching infrastructure are not migrated, being outside the unified GPU abstraction. The paper's Algorithm 3 priority carries both S_completion and S_unblock."
  ],
  "omissions": [
    "cache routing, prefix caching, model-replica idleness, autoscaling: outside this GPU abstraction",
    "S_unblock omitted, because the simulator has no model-server queue to attach it to; omega2 = 0",
    "local worker waiting-time aging is NOT migrated: the arm's hard service priority is a FIXED class (0/1) and ready_time is only a deterministic tie-break, so this reproduces Pythia's S_completion component, not its aging loop"
  ],
  "defects_closed_before_freeze": [
    "template.baseline used as the alphabet, which is not a role alphabet at all",
    "the consumer indexed by the last COMPLETED role while V excludes the role it is indexed by, double-counting the current candidate (80 instead of 60 on A->B->C with 10/20/40)",
    "the finite horizon was described as the paper's bound, which it is not"
  ],
  "prohibited_after_freeze": [
    "the role alphabet definition, including the dropped occurrence suffix",
    "PFA structure, transition semantics, pruning threshold and renormalisation",
    "DEFAULT_HORIZON and the expected-remaining recursion",
    "the current_role indexing semantics",
    "the Pythia-specific simulator interface",
    "the Algorithm 3 base priority (omega1/omega2), aging weight/scale, role_model mapping and the queue-demand proxy"
  ],
  "freeze_breaking_exception": "A reproducible correctness bug only, recorded as a freeze-breaking fix and never retuned to improve results.",
  "wording_correction": "Do not write that the finite horizon replaces a stationary solve: Pythia does not estimate this quantity by solving a stationary system. Write that it replaces the bounded-regex / repetition-bound representation.",
  "not_tunable_after_freeze": [
    "horizon",
    "role granularity",
    "pruning threshold",
    "priority form"
  ],
  "variant_rule": "Adjusting any of the above requires a NEW VARIANT; it must not overwrite the frozen baseline.",
  "freeze_breaking_fixes": [
    {
      "date": "2026-09-25",
      "defect": "The priority was ranked by a DURATION: the finite-horizon recursion accumulated per-role millisecond means and the live consumer added the candidate's runtime_p50 + load, making the arm a duration-weighted / SRTF-flavoured priority. Pythia Algorithm 3 uses the expected remaining DISTANCE in steps on the probabilistic regex.",
      "fix": "V(role) is now the expected NUMBER of future role transitions (a transition to <end> is not a role and is not counted) and S_completion = 1 / (1 + V(role)); the consumer adds no millisecond term. Profiler schema bumped to pythia-role-pfa-v2 so an ms-valued artifact fails closed.",
      "verification": "tests/test_pythia_fidelity.py adds test_the_distance_does_not_depend_on_durations and re-derives the chain/fan-out values in steps; 13/13 gate.",
      "not_a_retune": true
    }
  ],
  "V_plus_one_convention": "V counts future non-END roles AFTER the current request; therefore D_remaining = 1 + V(current_role).",
  "algorithm3_restoration_20260926": {
    "change": "Restored the FULL Algorithm 3 base priority omega1*S_completion + omega2*S_unblock plus worker-side aging, so the arm is no longer a completion-only variant.",
    "S_completion": "1/(1+V(role)), V = expected remaining distance in steps over the train-only role-PFA",
    "S_unblock": "mean over reachable future roles of S_completion(V(a)) when role a's train-mapped model has zero visible ready/running demand",
    "aging": "S_eff = S_base + lambda*(wait/tau), dimensionless",
    "frozen_constants": {
      "omega1": 1.0,
      "omega2": 1.0,
      "aging_weight": 1.0,
      "aging_scale_ms": 30000.0,
      "source": "pre-registered defaults; the paper states the form but gives no values"
    },
    "new_artifact_fields": [
      "role_model (train-only majority model per role)",
      "reachable_future_roles()"
    ],
    "schema": "pythia-role-pfa-v3",
    "non_degeneracy_probe": {
      "episodes": 3,
      "aging_changes_result": true,
      "S_unblock_changes_result": true,
      "S_unblock_nonzero_rate": "660/729 = 90.5%"
    },
    "sentinels": [
      "aging monotone in wait; dimensionless",
      "S_unblock: idle downstream raises it; unrelated model demand irrelevant; PFA mutation changes it",
      "role_model is train-only and validation-invariant",
      "reachable_future_roles excludes END and the start role"
    ]
  }
}
