# Forecast-aware Phase 2 评审记录（REV-20260911-FAS-P2-01）

日期：2026-09-11。Procedure：experiment-reviewer。原结论：CONDITIONAL PASS → modify（补丁，不重跑）。

## 主要发现与处理
1. **F1b（P1，形状混淆）**：J 为单场景 prob=1.0、按预测长度截断（14.2% 零步）；B05 为 3 场景×固定 5 步（首场景概率质量 0.606）。E2-vs-legacy 不能完全归因于预测质量。→ **已处理**：报告新增 §2b 形态对照表，跨 provider 结论降级为"制品级更优"，并列入 §6 形态匹配后续工作。
2. **F2d（P1，多重比较）**：15 个 CI 未校正。→ **已处理**：§2 注（主效应 Bonferroni 下仍显著；唯一含 0 的 E3b−E3a 本就 null）。
3. **F2e（P1，单种子边界）**：仅 J3:seed11 进矩阵；不得泛化为预测器族。→ **已处理**：§4 明示。
4. **F1e（P2，语义披露）**：`load_threshold=0.5` 与 E1 偏离未写。→ **已处理**：§0 披露。
5. **F5a（P1，可复现性）**：缺运行命令/环境/artifact 链。→ **已处理**：§0 补全命令、环境、构建链与两侧 manifest sha。
6. **F4a（P2）**：E2 收益的替代解释（形态）→ 同 F1b；**F4b（PASS）**：无实现 bug 解释（缓存隔离、CPU lane、阈值一致性均核查）。
7. **F3b（P2）**：evictions 反增未议 → **已加注**（E2 45.08 vs E0 41.06，收益非全面占优）。
8. **P2 杂项**：模板复用聚类未调整 → 已声明；`phase2_stats.py` latent bug → 另写 `phase2_stats_verify.py` 修正并复算一致。

## 保留的支持声明
- E2 拓扑显著改善：vs E0 −7.9%、vs legacy −6.0%（制品级）、三指标 CI 不含 0；
- 资源直接替换显著有害（+8.1% 相对 E2）；加载块增量≈0；
- oracle gap 14.7%（相对口径）。
