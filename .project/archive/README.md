# Archive

这里保存被当前 Project OS 控制面替代、但仍需追溯的旧控制文档。

- `CODEX_WORK_GUIDE_legacy_20260813.md`：旧版总指导，保留作为历史证据；其中根目录 `tracing/` 布局和旧阶段口径不再是当前执行规范。
- `docs/` 与 `tmp_docs/` 中的历史报告暂不移动，以保留原始实验叙述和引用；当前事实以 `.project/STATE.md`、当前 `docs/` 设计文档和 `data/manifests/` 登记为准。

归档不等于删除；除非另行确认，不清理这些文件。
